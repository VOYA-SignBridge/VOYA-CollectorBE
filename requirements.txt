# Minimal requirements inferred from imports in the project
# Edit versions to match your environment if needed.

fastapi>=0.95.0
uvicorn[standard]>=0.20.0
numpy>=1.24.0
opencv-python>=4.7.0
scipy>=1.9.0
# MediaPipe may be optional depending on installation platform
mediapipe>=0.10.0
# For keypoint/augmentation and other utilities
pillow>=9.0.0
# Celery + Redis for background worker
celery>=5.2.0
redis>=4.5.0
# Postgres driver
psycopg2-binary>=2.9.6
# For training scripts (choose either torch or tensorflow as needed)
# PyTorch (if you will train locally with torch)
torch>=2.0.0
torchvision>=0.15.0
# TensorFlow (optional; only needed if using train_model.py)
tensorflow>=2.12.0
# Scikit-learn for classical utilities
scikit-learn>=1.2.0
# For parsing multipart/form-data uploads
python-multipart>=0.0.6
# Optional extras
python-dotenv>=0.21.0
requests>=2.29.0
# Add your project's pinned versions here
